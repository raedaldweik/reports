# Anthropic API key — required for the chatbot to answer questions
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override the model
# MODEL=claude-sonnet-4-5-20250929

# Optional: directory containing alerts.csv / violations.csv
# (defaults to the repo root, falls back to https://raedaldweik.github.io/reports)
# DATA_DIR=/path/to/reports
